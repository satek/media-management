<%= simple_form_for(@link, 
      url: media_item_link_path(@media_item.id, @link.id)) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :url %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
